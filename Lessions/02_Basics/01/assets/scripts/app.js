const defaultValue = 0;
const calculationDescription = '(' + defaultValue + ' + 4) * 3 / 2 - 5 ** 2';

let currentResult = defaultValue;

currentResult = (((defaultValue + 8) * 3) / 2 - 5) ** 2;

outputResult(currentResult, calculationDescription);
